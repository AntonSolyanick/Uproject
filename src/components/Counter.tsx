import React, { FC, ReactElement, ReactNode } from "react";
import styles from "./Counter.module.scss";

const Counter: FC = () => {
  return <h1 className={styles.btn}>dwd</h1>;
};

export default Counter;
